изменяем po файл, и make all генериуем md файл.
сгенерировать окончательную документацию можно запустив утилиту doxygen
чтобы правильно все работало надо поправить в файле Doxyfile INPUT и OUTPUT_DIRECTORY,
хотя и так работает я указал там текущую директорию, т.е. .

